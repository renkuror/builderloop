[workspace]
members = [
  "crates/protocol-core",
  "programs/builderloop",
  "programs/cohort-build",
]
resolver = "2"

[workspace.package]
edition = "2021"
license = "Apache-2.0"
version = "0.1.0"

[profile.release]
opt-level = "s"
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
